<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Amazingpushycat" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Amazingpushycat" %>">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header>
      <%= render 'layouts/navigation' %>
    </header>
    <main role="main">
       <%= render 'layouts/messages' %>
       <%= yield %>
    </main>
    <!-- Footer -->
    <footer>
        <div class="row">
            <div class="col-lg-12 centered text-primary">
                <p>Copyright &copy; 2016 Amazing Pushy CAT Bank</p>
            </div>
        </div>

        <div class="row">
						<%= image_tag "logo.png", :title => "CA Titres", :class => "centered" %>
        </div>
    </footer>
  </body>
</html>
